<!-- ai-generated: 100% - Codex decomposed the accepted Lab 1 plan into verifiable implementation tasks -->
# Lab 1 implementation tasks

- [x] Add `requirements.txt` and `Dockerfile` with build-time dependency installation.
- [x] Add the `svcdesk` package and the `/health` endpoint.
- [x] Add the Ticket input/output models and JSON error handling.
- [x] Add SQLite schema initialization, creation, retrieval, and filtered listing.
- [x] Implement the complete impact/urgency matrix and C3 `vip` elevation.
- [x] Implement request-scoped `X-Test-Clock` parsing.
- [x] Implement the five transition endpoints and C2 `immutable` behavior.
- [x] Implement SLA targets, C1 `wallclock`, and DST-aware business-time addition.
- [x] Implement SLA breach and pause reporting.
- [x] Enable the Compose healthcheck and confirm there are no bind mounts.
- [x] Exercise API boundary cases locally.
- [x] Run `itsmlab verify 1 --json report.json` and resolve every Core failure.
